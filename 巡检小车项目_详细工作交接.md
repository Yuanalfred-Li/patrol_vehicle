# 巡检小车项目详细工作交接

> **交接日期**：2026-08-05  
> **交接对象**：后续开发 Agent  
> **当前项目工作空间**：`/home/nvidia/patrol_ws`  
> **底层驱动工作空间**：`/home/nvidia/ros2_humble_main`  
> **旧版工作空间（仅供追溯，禁止继续开发）**：`/home/nvidia/yuan_ws`  
> **最后确认分支**：`feature/persistent-runtime-status-gnss-hold`  
> **最后确认 HEAD**：`d36d651d39361ea0c771918af773689901c4f1aa`  
> **当前阶段**：核心路线录制、自动入轨、路线跟踪、任务管理、地图前端、GNSS 弱化保护和基础 CAN 控制已完成；正在验证“路线接管低速恢复 + 大转向动态降速”；尚不具备无人值守巡逻条件。

---

## 目录

1. [交接使用原则](#1-交接使用原则)
2. [项目最终目标](#2-项目最终目标)
3. [当前完成度](#3-当前完成度)
4. [硬件与系统环境](#4-硬件与系统环境)
5. [工作空间与代码边界](#5-工作空间与代码边界)
6. [系统总体架构与数据流](#6-系统总体架构与数据流)
7. [ROS 2 节点、话题、服务与状态机](#7-ros-2-节点话题服务与状态机)
8. [路线文件格式与坐标体系](#8-路线文件格式与坐标体系)
9. [路线录制、稳定起终点与平滑](#9-路线录制稳定起终点与平滑)
10. [地图前端与路线设计](#10-地图前端与路线设计)
11. [自动入轨规划与执行](#11-自动入轨规划与执行)
12. [正式路线跟踪与全部已确认参数](#12-正式路线跟踪与全部已确认参数)
13. [GNSS 弱化保持参数](#13-gnss-弱化保持参数)
14. [控制仲裁与底盘命令](#14-控制仲裁与底盘命令)
15. [启动脚本与标准操作](#15-启动脚本与标准操作)
16. [Git/GitHub 状态和提交历史](#16-gitgithub-状态和提交历史)
17. [已完成的测试和结论](#17-已完成的测试和结论)
18. [已知实车表现](#18-已知实车表现)
19. [当前未提交改动](#19-当前未提交改动)
20. [已知问题和风险](#20-已知问题和风险)
21. [后续 Agent 任务路线图](#21-后续-agent-任务路线图)
22. [实车测试纪律与验收建议](#22-实车测试纪律与验收建议)
23. [Agent 接手第一天清单](#23-agent-接手第一天清单)
24. [常用命令速查](#24-常用命令速查)
25. [必须现场补充的信息](#25-必须现场补充的信息)
26. [最终交接结论](#26-最终交接结论)

---

# 1. 交接使用原则

## 1.1 当前源代码唯一基准是 `/home/nvidia/patrol_ws`

早期项目使用过：

```text
/home/nvidia/yuan_ws
```

其中包含旧版：

- `localization_yuan`
- `route_yuan`
- `waypoint_nav_yuan`
- `obstacle_guard_yuan`
- `bringup_yuan`
- `/cmd_vel` 控制链
- 旧路线和调试脚本

这些内容只允许用于追溯历史问题，不允许继续开发。

后续 Agent 禁止：

- 在 `/home/nvidia/yuan_ws` 增加新功能；
- source 旧工作空间；
- 运行旧代码生成脚本覆盖当前代码；
- 恢复旧 `/cmd_vel` 控制链；
- 把旧参数直接复制到当前工程；
- 把旧路线测试结论当作当前系统状态。

每个新终端只加载：

```bash
source /opt/ros/humble/setup.bash
source /home/nvidia/ros2_humble_main/install/setup.bash
source /home/nvidia/patrol_ws/install/setup.bash
```

## 1.2 “最后确认”不等于“此刻仍然不变”

本文件中的分支、HEAD、工作区状态和参数均来自最后一次确认。

Agent 接手后必须先执行：

```bash
cd /home/nvidia/patrol_ws

git remote -v
git branch -vv
git status --short
git log --graph --decorate --oneline --all -60
git diff --stat
```

若结果与本文件不同，以 Jetson 当前仓库为准，并在本文件末尾追加“现场接手补充记录”。

## 1.3 硬件事实不明确时禁止猜测

以下内容必须依据协议、源码、原始 CAN 或现场测量：

- CAN 报文字节序；
- 字段偏移；
- 补码规则；
- 比例系数；
- `±400` 与前轮角度关系；
- RPM 与真实车速关系；
- 转向执行器延迟、死区、回差；
- 电池电压、电流、SOC；
- VCU/BMS 转发方式；
- MINS200 状态字段；
- Livox 消息类型；
- 后向传感器能力。

不得使用“通常车辆会这样”作为修改依据。

## 1.4 所有实车测试必须保留人工接管

当前系统仍存在：

- Hybrid A* 无障碍物碰撞地图；
- 无道路边界；
- 自动入轨可能倒车；
- 无可靠后向障碍检测；
- Livox 避障闭环未完成；
- 转向执行器未完成真实标定；
- GNSS 弱化保持仍需更多实车回归。

实车测试必须：

- 空旷、封闭、无人员和车辆；
- 一人操作，一人监护；
- 遥控器在手；
- 硬件急停可用；
- 前后确认无障碍；
- 首次低速短距离；
- 发现异常立即停车；
- 不允许“让算法继续跑看看能否恢复”。

---

# 2. 项目最终目标

## 2.1 业务目标

最终形成一套运行在 Jetson + ROS 2 Humble 上的室外巡检小车系统，能够：

1. 接入 MINS200 GNSS、航向和 IMU；
2. 将 WGS84 转换为每条路线独立的 ENU；
3. 通过手动驾驶录制巡检路线；
4. 对路线做异常点处理、平滑、端点稳定和几何检查；
5. 在车辆不完全位于路线起点时自动规划入轨路径；
6. 按前轮转向车辆约束执行前进、倒车和换向；
7. 入轨后使用 Pure Pursuit 跟踪指定路线；
8. 可靠判断终点并停车；
9. GNSS 短时弱化时进行严格受限的航向保持；
10. GNSS 无法安全恢复时停车；
11. 通过唯一控制仲裁向底盘发送命令；
12. 在网页地图显示车辆、航向、实际轨迹、路线和任务状态；
13. 接入 Livox MID360 完成前向障碍减速、停车；
14. 增加后向障碍检测；
15. 接入摄像头进行录像和异常识别；
16. 支持多路线、任务切换、状态监控、日志和故障追踪；
17. 最终实现可重复、可监控、可安全停止的无人巡检。

## 2.2 当前技术路线

```text
MINS200 GNSS/INS
→ 固定路线原点 ENU
→ Hybrid A* 自动入轨
→ 入轨执行器
→ Pure Pursuit 正式巡迹
→ STOP/MANUAL/AUTO 仲裁
→ VehicleCommand
→ CAN / VCU
```

当前不是：

- 二维激光 SLAM 主定位；
- Nav2 主导航；
- Autoware；
- S100P 项目；
- 直接 `/cmd_vel` 控制。

---

# 3. 当前完成度

| 功能 | 状态 | 备注 |
|---|---|---|
| MINS200 数据接入 | 已完成 | GPS、航向、IMU 链路已建立 |
| WGS84 → ENU | 已完成 | 每条路线独立 origin |
| 稳定起点采样 | 已完成 | 静止约 5 秒，异常点剔除 |
| 稳定终点采样 | 已完成 | 录制结束后再次采样 |
| 路线录制 | 已完成 | `format_version: 2` |
| 路线平滑 | 已完成基础版 | 可保留 raw 后重复生成 |
| 路线坐标一致性检查 | 已完成 | 经纬度与缓存 x/y 对比 |
| 地图路线编辑 | 已完成基础版 | 高德地图、开放/闭环路线 |
| Hybrid A* 入轨规划 | 已完成 | 支持倒车和换向 |
| 入轨闭环执行 | 已完成 | 车辆式 Pure Pursuit |
| 正式路线跟踪 | 已完成基础版 | 折线投影 + 单调弧长 |
| 终点判断 | 已完成基础版 | 剩余弧长 + 终点距离 |
| STOP/MANUAL/AUTO | 已完成 | command manager |
| 自动命令复用 | 已完成 | ENTRY/ROUTE mux |
| 任务状态机 | 已完成 | 规划、入轨、巡迹、成功/失败 |
| 动态路线加载 | 已完成 | 失败可回滚 |
| 上层常驻运行时 | 已完成 | 单任务停止不退出所有节点 |
| 地图任务状态 | 已完成 | 可显示任务进度和结果 |
| GNSS 短时弱化保护 | 已实现 | 仍需实车验证 |
| 安全 CAN 发送控制 | 已加强 | 显式发送开关、控制源收敛 |
| 路线接管动态限速 | 已实现未提交 | 仿真有效，待完整回归 |
| 障碍物碰撞地图 | 未完成 | Hybrid A* 仍不看障碍 |
| Livox 动态避障 | 未完成验收 | 不得视为已具备 |
| 后向障碍检测 | 未完成 | 倒车风险 |
| 摄像头异常识别 | 未完成 | 目前只有硬件基础 |
| 无人值守运行 | 未达到 | 禁止无人外场测试 |

---

# 4. 硬件与系统环境

## 4.1 Jetson

最后确认环境：

| 项目 | 信息 |
|---|---|
| 平台 | Jetson Orin NX 8GB 级别 |
| JetPack | 6.2.1 |
| Ubuntu | 22.04.4 |
| ROS | ROS 2 Humble |
| Python | 3.10.12 |
| CUDA | 12.6 |
| PyTorch | 2.5.0a0+nv24.08 |
| 内存 | 约 7.4 GiB |
| NVMe | 约 116 GB |

Agent 接手时重新保存：

```bash
uname -a
lsb_release -a
python3 --version
printenv ROS_DISTRO
df -h
free -h
```

## 4.2 车辆底盘

| 参数 | 当前已确认值 |
|---|---|
| 车辆宽度 | 约 `0.80 m` |
| 车辆长度 | 约 `1.35 m` |
| 轴距 | `0.67 m` |
| 转向形式 | 前轮转向，后轮不转向 |
| 最大机械转角 | 约 `±30°` |
| 速度输入 | RPM |
| 转向输入 | `-400 ～ +400` 请求量 |
| 转向符号 | 左正、右负 |
| 原地转向 | 不支持 |
| 自动入轨 | 可能倒车、换向 |

特别注意：

```text
±400 不是已完成标定的前轮角度。
```

当前控制模型采用近似线性映射，但真实关系必须实测。

## 4.3 MINS200

用途：

- 纬度、经度、海拔；
- 东、北、天速度；
- Pitch、Roll、Yaw；
- 陀螺仪、加速度；
- NSV1、NSV2；
- 对准状态、GNSS 状态、组合导航状态。

网络地址：

```text
192.168.1.33
```

协议 yaw：

```text
北向 0°，北偏东为正，0～360°
```

ROS ENU yaw 转换必须保持符号和零点正确。

## 4.4 Livox MID360

历史出现的话题：

```text
/livox/lidar
/cloud_registered
/cloud_registered_body
/cloud_effected
/Laser_map
/scan
```

重要要求：

```bash
ros2 topic info -v /livox/lidar
ros2 topic info -v /scan
```

必须确认真实类型和发布频率。

不得仅凭“订阅成功”日志认为传感器数据已进入避障节点。

## 4.5 云台网络摄像头

历史确认：

| 项目 | 值 |
|---|---|
| IP | `192.168.1.68` |
| 用户名 | `admin` |
| RTSP | `554` |
| HTTP | `80/8000` 曾开放 |
| 云台 | RS485 |
| 云台波特率 | `2400` |

`2400` 只用于云台控制，不是视频码率。

密码、RTSP 路径和认证方式应通过安全配置管理，不得提交明文到公共仓库。

## 4.6 CAN

| 项目 | 值 |
|---|---|
| 接口 | `can0` |
| 波特率 | `500000` |
| 底层节点 | `/vehicle_interface_node` |
| 控制话题 | `/vehicle/command` |
| 状态话题 | `/vehicle/status` |
| 正常状态 | `ERROR-ACTIVE` |

实车前：

```bash
ip -details -statistics link show can0
```

禁止在 `DOWN`、`STOPPED` 或未知状态运行自动任务。

已知风险：

- `0x701` 速度/转向反馈解析可能错误；
- `0x703` 电池状态解析可能错误；
- 电流、SOC、转向反馈在修复前不可作为可信闭环数据。

---

# 5. 工作空间与代码边界

## 5.1 底层驱动工作空间

```text
/home/nvidia/ros2_humble_main
```

包含：

- `vehicle_can_interface`
- MINS200 驱动
- `livox_ros_driver2`
- 机器人状态发布
- 厂家和平台驱动

职责：

```text
设备连接、驱动、消息、CAN 收发
```

修改原则：

- 先查协议和原始数据；
- 改动单独分支；
- 尽量参数化；
- 保留兼容和回滚；
- 禁止无证据重写 CAN 协议。

## 5.2 当前项目工作空间

```text
/home/nvidia/patrol_ws
```

主要包和目录：

| 包/目录 | 作用 |
|---|---|
| `patrol_interfaces` | 自定义消息、服务 |
| `patrol_localization` | 定位与质量状态 |
| `patrol_route_recorder` | 路线录制 |
| `patrol_entry_planner` | Hybrid A* 入轨规划 |
| `patrol_entry_executor` | 入轨路径执行 |
| `patrol_route_follower` | Pure Pursuit、GNSS hold、动态限速 |
| `patrol_auto_command_mux` | ENTRY/ROUTE 自动命令选择 |
| `patrol_command_manager` | STOP/MANUAL/AUTO 仲裁 |
| `patrol_mission_manager` | 任务状态机 |
| `patrol_bringup` | 启动和参数映射 |
| `config/` | 统一配置 |
| `scripts/` | 启停、录制、复现、状态 |
| `tools/` | 地图、检查、控制、测试 |
| `routes/` | 路线 YAML |
| `reports/` | 报告和 HTML |
| `logs/` | 持久化日志 |

---

# 6. 系统总体架构与数据流

## 6.1 定位链路

```text
/gps/data + /imu/status
           │
           ▼
patrol_localization
├── /patrol/localization_status
├── /patrol/pose
└── /patrol/odom
```

定位无效时：

- `valid=false`；
- 自动控制不应继续使用陈旧位姿；
- 路线跟踪器停车或进入严格限制的 hold；
- 超时必须停止。

## 6.2 自动任务链路

```text
patrol_mission_manager
├── patrol_entry_planner
├── patrol_entry_executor
└── patrol_route_follower
```

命令：

```text
/patrol/entry_command
/patrol/route_command
        │
        ▼
patrol_auto_command_mux
        │
        ▼
/patrol/auto_command
```

## 6.3 最终仲裁

```text
/patrol/manual_command
/patrol/auto_command
        │
        ▼
patrol_command_manager
        │
        ▼
/vehicle/command
        │
        ▼
vehicle_interface_node
        │
        ▼
CAN / VCU
```

真实车辆必须满足：

```text
/vehicle/command Publisher count == 1
发布者 == patrol_command_manager
```

离线仿真必须改为：

```text
/patrol/test_vehicle_command
```

## 6.4 地图链路

```text
/patrol/localization_status
/patrol/mission/status
        │
        ▼
tools/patrol_map_server.py
        │
        ▼
HTTP 地图前端
```

地图前端不直接控制底盘，任务仍走完整任务管理链路。

---

# 7. ROS 2 节点、话题、服务与状态机

## 7.1 底层节点

```text
/smins200_tcp_demo
/vehicle_interface_node
/livox_lidar_publisher
/robot_state_publisher
```

## 7.2 上层节点

```text
/patrol_localization
/patrol_route_recorder
/patrol_entry_planner
/patrol_entry_executor
/patrol_route_follower
/patrol_auto_command_mux
/patrol_command_manager
/patrol_mission_manager
```

## 7.3 关键话题

| 话题 | 用途 |
|---|---|
| `/gps/data` | MINS200 GPS |
| `/imu/status` | 航向/IMU 状态 |
| `/patrol/localization_status` | 定位质量和绝对/局部坐标 |
| `/patrol/pose` | 控制位姿 |
| `/patrol/odom` | 定位里程计 |
| `/patrol/recorded_path` | 录制路径 |
| `/patrol/entry_path` | 入轨路径 |
| `/patrol/entry_command` | 入轨命令 |
| `/patrol/route_command` | 路线命令 |
| `/patrol/auto_command` | 自动命令 |
| `/patrol/manual_command` | 手动命令 |
| `/patrol/control_mode` | 控制模式 |
| `/patrol/mission/status` | 总任务状态 |
| `/patrol/entry_executor/status` | 入轨状态 |
| `/patrol/route_follower/status` | 跟踪状态 |
| `/patrol/target_path` | 目标路线 |
| `/patrol/lookahead_target` | 前视目标 |
| `/vehicle/command` | 实车最终命令 |
| `/vehicle/status` | 车辆反馈 |
| `/livox/lidar` | MID360 数据 |
| `/scan` | 可能存在的 LaserScan |

## 7.4 关键服务

```text
/patrol/route_recorder/start
/patrol/route_recorder/stop
/patrol/entry_planner/plan
/patrol/entry_executor/enable
/patrol/route_follower/enable
/patrol/set_control_mode
/patrol/mission/start
/patrol/mission/stop
/patrol/mission/load_route
/patrol/route_follower/load_route
```

运行时以：

```bash
ros2 service list | grep /patrol
```

为准。

## 7.5 控制模式

```text
STOP   = 0
MANUAL = 1
AUTO   = 2
```

## 7.6 任务状态机

```text
IDLE
PLANNING
STARTING_ENTRY
ENTRY
STARTING_ROUTE
ROUTE
SUCCEEDED
FAILED
```

动态加载：

```text
LOADING_ROUTE_FOLLOWER
LOADING_ENTRY_PLANNER
LOADING_LOCALIZATION
ROLLING_BACK_ROUTE
```

典型流程：

```text
IDLE
→ PLANNING
→ STARTING_ENTRY
→ ENTRY
→ STARTING_ROUTE
→ ROUTE
→ SUCCEEDED
```

任一阶段失败：

- 禁用执行器和跟踪器；
- 发布 FAILED；
- 切回 STOP；
- 不保留旧速度命令。

---

# 8. 路线文件格式与坐标体系

实车路线要求：

```yaml
format_version: 2

origin:
  latitude: 34.x
  longitude: 113.x
  altitude: 100.x
  yaw_deg: 35.2

origin_estimation:
  ...

summary:
  point_count: 175
  total_length: 69.574
  closed_loop: false

waypoints:
  - index: 0
    latitude: ...
    longitude: ...
    altitude: ...
    x: ...
    y: ...
    z: ...
    yaw_deg: ...
```

复现脚本拒绝：

- `format_version < 2`；
- origin 缺失；
- origin 为 `0,0`；
- 经纬度越界；
- 航点少于 2；
- 航点缺少绝对坐标；
- 路线名包含 `/` 或 `..`。

坐标原则：

- WGS84 是绝对依据；
- x/y 是缓存；
- 加载时应按 origin 重新计算 ENU；
- 不得盲信缓存 x/y；
- 高德地图显示使用 GCJ-02；
- 后端负责 GCJ-02/WGS84 转换。

---

# 9. 路线录制、稳定起终点与平滑

## 9.1 路线录制

```bash
cd /home/nvidia/patrol_ws
./scripts/record_route.sh 路线名称
```

流程：

```text
检查 MINS200/CAN
→ 静止约 5 秒
→ 起点位置中位数筛选
→ MAD 异常点剔除
→ ECEF 平均
→ 航向圆周平均
→ RMS/航向标准差检查
→ 固定 origin
→ 开始记录
→ 键盘驾驶
→ 停车退出
→ 静止约 5 秒
→ 稳定终点采样
→ 保存 raw
→ 平滑生成正式路线
```

键盘：

```text
Q/W/E：左前 / 前进 / 右前
A/S/D：左转 / 停止 / 右转
Z/X/C：左后 / 后退 / 右后
空格：停止
英文句号：退出
```

## 9.2 路线平滑参数

配置：

```text
/home/nvidia/patrol_ws/config/route_smoothing.yaml
```

以下为历史确认/常用值，Agent 必须读取当前 YAML 复核：

| 参数 | 值 | 含义 |
|---|---:|---|
| `spacing_m` | `0.40` | 重采样点间距 |
| `sigma_m` | `0.80` | 平滑空间尺度 |
| `strength` | `0.85` | 平滑融合强度 |
| `maximum_shift_m` | `0.35` | 平滑偏移安全上限 |
| `endpoint_hold_m` | `2.00` | 起终点锚定影响距离 |
| `heading_window_distance_m` | `1.50` | 航向计算窗口 |
| `maximum_anchor_shift_m` | `1.50` | 锚点质量检查阈值 |
| `spike_threshold_m` | `0.20` | 回摆点距离阈值 |
| `spike_angle_deg` | `100.0` | 回摆点角度阈值 |

### 参数调节原则

`maximum_shift_m` 是重要安全上限：

- 路线切弯：减小；
- 定位噪声：先查 GNSS，不要盲目增大；
- 非空旷区域不建议超过 `0.45 m`。

`heading_window_distance_m` 不是控制前视距离。

`maximum_anchor_shift_m` 是数据质量阈值，不是平滑力度。

## 9.3 重新生成

```bash
./scripts/smooth_route.sh 路线名_raw 路线名
```

必须保留 `_raw.yaml`。

---

# 10. 地图前端与路线设计

启动：

```bash
./scripts/start_patrol_map.sh
```

默认：

```text
http://JETSON_IP:8080
```

功能：

- 标准图/卫星图；
- 车辆实时位置；
- 航向；
- 实际轨迹；
- 保存路线显示；
- 开放/闭环路线；
- 关键点绘制；
- 节点编辑；
- 曲线预览；
- 点间距；
- 最小转弯半径；
- tension；
- 平滑迭代；
- 自交检查；
- 曲率检查；
- 路线保存；
- 任务启动/停止；
- 任务状态。

地图设计历史默认：

| 参数 | 值 |
|---|---:|
| `spacing_m` | `0.40` |
| `minimum_turning_radius_m` | `1.50` |
| `tension` | `0.15` |
| `smoothing_iterations` | `3` |
| dense spacing | 约 `0.03 m` |

保存前检查：

- 自交；
- 最小半径；
- 路线长度；
- 点数；
- GCJ→ENU 校准误差；
- 路线是否偏离可行道路。

---

# 11. 自动入轨规划与执行

## 11.1 Hybrid A*

输入：

- 当前 ENU；
- 当前 yaw；
- 路线起点和切向；
- 轴距；
- 转弯约束。

输出：

```text
/patrol/entry_path
```

支持：

- 前进；
- 倒车；
- 换向。

不支持：

- 障碍物碰撞检查；
- 道路边界；
- 后向安全。

因此“规划成功”不代表现场安全。

## 11.2 入轨执行器全部已确认参数

| 参数 | 值 | 含义 |
|---|---:|---|
| `control_rate_hz` | `20.0` | 控制频率 |
| `forward_speed_rpm` | `15.0` | 前进速度 |
| `reverse_speed_rpm` | `10.0` | 倒车速度 |
| `wheelbase_m` | `0.67` | 轴距 |
| `lookahead_distance_m` | `0.40` | 入轨前视距离 |
| `maximum_tracking_error_m` | `0.80` | 入轨安全误差 |
| `maximum_mechanical_steering_deg` | `30.0` | 模型机械上限 |
| `maximum_steering_request` | `400.0` | 最大请求 |
| `waypoint_tolerance_m` | `0.08` | 路径点容差 |
| `maximum_pass_lateral_error_m` | `0.80` | 判定路径点通过的横向上限 |
| `maximum_start_error_m` | `0.60` | 启动时距路径起点上限 |
| `gear_change_pause_sec` | `0.80` | 换向等待 |
| `maximum_localization_age_sec` | `0.50` | 定位最大年龄 |

输入：

```text
/patrol/entry_path
/patrol/pose
/patrol/localization_status
```

输出：

```text
/patrol/entry_command
```

---

# 12. 正式路线跟踪与全部已确认参数

## 12.1 算法

当前路线跟踪使用 Pure Pursuit，已从旧版最近航点索引升级为：

1. 当前位置投影到路线折线段；
2. 得到 `projection_s`；
3. `progress_s` 单调推进；
4. `target_s = progress_s + lookahead`；
5. 插值获取前视点；
6. 计算车辆到前视点的方向差；
7. 计算前轮模型转角；
8. 映射为 `-400～400`；
9. 误差过大安全停车；
10. 剩余弧长和终点距离同时满足才完成。

## 12.2 话题与服务

| 名称 | 值 |
|---|---|
| pose | `/patrol/pose` |
| localization status | `/patrol/localization_status` |
| command | `/patrol/route_command` |
| target path | `/patrol/target_path` |
| lookahead target | `/patrol/lookahead_target` |
| status | `/patrol/route_follower/status` |
| load route service | `/patrol/route_follower/load_route` |

## 12.3 当前基础参数

| 参数 | 当前值 | 含义 |
|---|---:|---|
| `control_rate_hz` | `20.0` | 控制频率 |
| `lookahead_distance_m` | `1.50` | 正式路线前视距离 |
| `wheelbase_m` | `0.67` | 轴距 |
| `maximum_mechanical_steering_deg` | `30.0` | 控制模型机械角 |
| `maximum_steering_request` | `400.0` | 最大底盘请求 |
| `minimum_speed_rpm` | `10.0` | 正常最低速度 |
| `maximum_speed_rpm` | `15.0` | 当前最高速度 |
| `slowdown_distance_m` | `2.0` | 终点减速距离 |
| `maximum_entry_path_error_m` | `1.0` | 允许启用跟踪的横向误差 |
| `maximum_entry_heading_error_deg` | `180.0` | 当前过宽松，待标定 |
| `maximum_tracking_error_m` | `1.5` | 跟踪安全停车阈值 |
| `final_tolerance_m` | `0.30` | 终点弧长/距离容差 |
| `maximum_localization_age_sec` | `0.50` | 位姿最大年龄 |

特别说明：

```text
maximum_entry_heading_error_deg = 180°
不是最终安全值，只是当前遗留宽松值。
```

## 12.4 新增动态限速参数（未提交）

| 参数 | 值 | 含义 |
|---|---:|---|
| `handoff_recovery_distance_m` | `3.0` | 接管后低速恢复距离 |
| `handoff_recovery_speed_rpm` | `8.0` | 接管起始速度 |
| `steering_slowdown_start_request` | `180.0` | 从此转向量开始降速 |
| `steering_slowdown_full_request` | `320.0` | 到此转向量降到最低 |
| `steering_slowdown_speed_rpm` | `8.0` | 大转向最低速度 |

速度逻辑：

```text
基础速度：
  距终点较远 → maximum_speed
  进入终点减速距离 → minimum～maximum 插值

接管恢复上限：
  0 m → 8 RPM
  3 m → 15 RPM

转向上限：
  |steer| <= 180 → 不限
  180～320 → 15→8 RPM
  >=320 → 8 RPM

最终速度：
  所有上限的最小值
```

日志原因：

```text
speed_limit=normal
speed_limit=endpoint
speed_limit=handoff_recovery
speed_limit=steering_slowdown
```

---

# 13. GNSS 弱化保持参数

以下参数在 `route_follower_node.py` 中已声明。实际运行值必须用当前 YAML 和 `ros2 param dump` 复核。

| 参数 | 默认/已知值 | 含义 |
|---|---:|---|
| `gnss_degraded_enabled` | `False` | 是否启用 |
| `gnss_weak_debounce_sec` | `0.3` | 弱化去抖 |
| `gnss_maximum_hold_sec` | `3.0` | 最长保持 |
| `gnss_hold_speed_rpm` | `8.0` | 保持速度 |
| `gnss_maximum_heading_error_deg` | `8.0` | 最大航向误差 |
| `gnss_heading_control_kp` | `8.0` | 航向比例增益 |
| `gnss_maximum_steering_request` | `80.0` | 保持最大转向 |
| `gnss_maximum_imu_age_sec` | `0.5` | IMU 最大年龄 |
| `gnss_maximum_entry_steering_request` | `80.0` | 进入保持前最大转向 |
| `gnss_minimum_straight_segment_length_m` | `4.0` | 最小直线长度 |
| `gnss_maximum_straight_heading_change_deg` | `5.0` | 直线方向变化限制 |
| `gnss_recovery_stable_sec` | `1.0` | 恢复稳定时间 |
| `gnss_recovery_max_path_error_m` | `1.0` | 恢复最大路径误差 |
| `gnss_recovery_max_heading_error_deg` | `15.0` | 恢复最大航向误差 |
| `gnss_recovery_max_progress_jump_m` | `2.0` | 恢复进度跳变上限 |
| `gnss_auto_resume_after_stop` | `False` | 停车后自动恢复 |

安全原则：

- 只允许短时；
- 只允许直线；
- 只允许低速；
- IMU 必须新鲜；
- 大转向时不进入；
- 超时停车；
- 恢复需稳定验证；
- 不允许在弯道盲走。

---

# 14. 控制仲裁与底盘命令

## 14.1 Auto command mux 参数

| 参数 | 值 |
|---|---|
| `entry_command_topic` | `/patrol/entry_command` |
| `route_command_topic` | `/patrol/route_command` |
| `output_command_topic` | `/patrol/auto_command` |
| `entry_status_topic` | `/patrol/entry_executor/status` |
| `route_status_topic` | `/patrol/route_follower/status` |
| `command_timeout_sec` | `0.50` |
| `publish_rate_hz` | `20.0` |

## 14.2 Command manager

职责：

- STOP/MANUAL/AUTO；
- 接收手动和自动命令；
- 发布唯一最终命令；
- 命令超时停车；
- 启动默认 STOP；
- 退出时发布停车。

已知关闭竞态：

```text
RCLError: Failed to publish:
publisher's context is invalid
```

建议修复：

- 发布前检查 `rclpy.ok()`；
- 捕获 `RCLError`；
- context 失效后不再发布；
- 避免 shutdown 重入；
- 保留正常停车发布。

---

# 15. 启动脚本与标准操作

## 15.1 启动底层

```bash
cd /home/nvidia/patrol_ws
./scripts/start_base.sh
```

功能：

- 加载环境；
- 检查 MINS200 网络；
- 防止重复节点；
- 启动 MINS200；
- 启动 CAN；
- 检查 CAN；
- 保存日志/PID。

## 15.2 手动控制

```bash
./scripts/manual_control.sh
```

或：

```bash
./tools/patrol_control.sh manual
```

## 15.3 路线录制

```bash
./scripts/record_route.sh 路线名称
```

## 15.4 路线列表

```bash
./scripts/list_routes.sh
```

## 15.5 路线复现

```bash
./scripts/replay_route.sh 路线名称
```

可选：

```bash
./scripts/replay_route.sh 路线名称 --yes
```

首次实车不建议跳过人工确认。

## 15.6 停止任务

```bash
./scripts/stop_mission.sh
```

上层常驻运行时继续存在。

## 15.7 查看状态

```bash
./scripts/status.sh
```

## 15.8 完全关闭

```bash
./scripts/stop_all.sh
```

## 15.9 地图

```bash
./scripts/start_patrol_map.sh
```

---

# 16. Git/GitHub 状态和提交历史

## 16.1 最后确认状态

```text
分支：feature/persistent-runtime-status-gnss-hold
HEAD：d36d651d39361ea0c771918af773689901c4f1aa
远端分支：origin/feature/persistent-runtime-status-gnss-hold
备份引用：backup/before-0728-audit-20260802_195031
```

本次生成交接时 GitHub 连接器没有返回可访问仓库，因此：

- 未能确认 `owner/repo`；
- 未能核验 PR；
- 未能核验 Issue；
- 未能核验 Actions；
- 以下历史来自本地 Git 快照。

Agent 必须首先执行：

```bash
git remote -v
```

## 16.2 分支演进

```text
feature/auto-route-smoothing
→ feature/map-route-replay-ui
→ feature/persistent-runtime-status-gnss-hold
```

## 16.3 提交历史

### 基础架构

| SHA | 时间 | 提交说明 |
|---|---|---|
| `6c1f239b1281195bcf4ba89430d038fb396fb87c` | 2026-07-20 10:49 | Initial patrol workspace skeleton |
| `7e06286a2df53bcc80827a481a1b4e14e59d2d57` | 2026-07-20 14:42 | Add initial pure pursuit route follower |
| `452fff40b965e015670f12a5b462cd2b6ec0d996` | 2026-07-20 15:21 | Add Hybrid A star entry path planner |
| `182149eb15133960ca29b5aeb812e0e1a021ba5a` | 2026-07-20 15:35 | Add entry path executor with gear switching |
| `2bd6be1564c1d3c55068620ecee82bd5877b07a6` | 2026-07-20 16:00 | Complete automatic entry and route mission flow |
| `5fcba5319e061eaf985d414150e842ed2946a7b5` | 2026-07-20 16:12 | Add automatic command multiplexer |
| `1dcdc3b47ff7043544340193a8483b1c415e69b5` | 2026-07-20 18:05 | Tighten route completion tolerance |

### 启动、录制、绝对坐标

| SHA | 时间 | 提交说明 |
|---|---|---|
| `2373117c6464e4e137905489f24ced084039e832` | 2026-07-21 09:53 | Add autonomous patrol bringup and clean shutdown |
| `35cc8189c3a0dec61d3e6ad494e4ebd920d57aef` | 2026-07-21 10:03 | Add route recorder to patrol bringup |
| `82df9de504495fb692374be80044ffd297fb3179` | 2026-07-21 10:08 | Rename upper-layer patrol launch |
| `82a6e62aa8d0996cd355399ed3769c0a1e15c14e` | 2026-07-21 10:26 | Add hardware and full patrol launch |
| `06c66dc1b79898f0d0911dfa66e5f20183b9838c` | 2026-07-21 11:38 | Load patrol route from absolute coordinates |
| `4d9f399950348a823d3c855e4f9bdbdbc986a9df` | 2026-07-21 16:55 | Add route recording and replay scripts |

### 路线平滑和地图

| SHA | 时间 | 提交说明 |
|---|---|---|
| `675b00a33942b111b02820eddb988922690975e0` | 2026-07-27 16:04 | Add map route editor and field test tools |
| `f632cf8ee2f8f675786978dbf9a1aad897b18b13` | 2026-07-28 10:39 | Improve vehicle marker route drawing |
| `621a03409bf7391257cdb6b3d8f7cc69dabe9217` | 2026-07-28 10:49 | Move map coordinate conversion to backend |
| `d9b97ec93c447a22c5c0fd998d69e791b88bd861` | 2026-07-28 11:11 | Add safe non-interactive route replay |

### 常驻运行时、状态和 GNSS

| SHA | 时间 | 提交说明 |
|---|---|---|
| `3ccff018ade151515ee55506cd7c184d0fd704b2` | 2026-07-30 15:06 | Add map replay controls and protect route data |
| `2bd1f4792fb219cf5128a20732dcaa8eb597dac4` | 2026-07-30 16:03 | Centralize patrol runtime configuration |
| `ae6f12b309ec15c75d6eb735f0ff051c9c39a0ac` | 2026-07-30 18:18 | Add coordinated dynamic route loading |
| `1db23ecbd9a33ef0d96e27396b8677821af09282` | 2026-07-30 18:55 | Keep patrol runtime alive between missions |
| `3c76be24a3fc105a1aab3e56f1ceed819bc4b25a` | 2026-07-30 19:37 | Show live patrol mission status in map frontend |
| `e1010d5d8b77bb3eec99acf01ae41f6d52772ad8` | 2026-07-30 20:08 | Add structured GNSS degradation state model |
| `3789d48ac90b081fbd0db2a9e2717e541a6d5232` | 2026-07-30 21:12 | Integrate guarded GNSS heading hold |
| `61d89b09217551a39042d848f1c9d048ffce47fe` | 2026-07-30 21:19 | Enable guarded GNSS heading hold for field test |

### 底层兼容和外场强化

| SHA | 时间 | 提交说明 |
|---|---|---|
| `24ca92acfcc91a75ea16ca0e63071892673e9e4e` | 2026-07-31 09:55 | Handle missing MINS200 navigation status field |
| `d610ccaa74b2288edfdf144f3cbbe81fa33254bd` | 2026-07-31 17:46 | Enable safe CAN actuation and harden patrol control |
| `d36d651d39361ea0c771918af773689901c4f1aa` | 2026-08-01 10:04 | `0731`，说明过于简略，需 `git show` |

---

# 17. 已完成的测试和结论

## 17.1 基础离线完整任务

已验证：

- Hybrid A*；
- 前进、倒车、换向；
- 入轨闭环；
- 路线跟踪；
- 自动命令复用；
- 任务状态机；
- 终点停车；
- 绝对坐标路线。

历史通过：

```text
MISSION_SUCCEEDED: True
ROUTE_END_ERROR: 0.227 m
ROUTE_HEADING_ERROR_DEG: 7.031°
TEST_RESULT: PASS
```

## 17.2 `/patrol/pose` 启动竞态

曾出现：

```text
entry executor start failed:
cannot enable entry executor: no pose
```

已修改测试：

- 订阅 `/patrol/pose`；
- 至少 3 个 pose sample；
- 准备等待增加到约 2 秒。

## 17.3 路线速度调整

从：

```text
12～20 RPM
```

调整到：

```text
10～15 RPM
```

直线和弯道仿真通过。

## 17.4 路线几何

### `7.28_test_02.yaml`

| 指标 | 值 |
|---|---:|
| 点数 | 80 |
| 长度 | 31.217 m |
| 最小约 1 m 窗口半径 | 1.507 m |
| 急弯位置 | progress 约 15.41 m |

实车饱和区域约 14.77～18.27 m，与急弯吻合。

### `7.31_test_01.yaml`

| 指标 | 值 |
|---|---:|
| 点数 | 175 |
| 长度 | 69.573～69.574 m |
| 最小约 1 m 窗口半径 | 6.084 m |
| 位置 | progress 约 43.18 m |

该路线较平滑，接管后立即偏离不能简单归因于路线曲率。

## 17.5 弯道仿真

### 半径 2 m、15 RPM

```text
MAX_ABS_ROUTE_STEERING: 275.121
饱和：0
MAX_ROUTE_PATH_ERROR: 0.269 m
PASS
```

### 半径 1.5 m、15 RPM

```text
MAX_ABS_ROUTE_STEERING: 339.796
饱和：0
MAX_ROUTE_PATH_ERROR: 0.267 m
PASS
```

结论：

```text
1.5 m 半径在理想模型中不会必然造成 ±400。
```

## 17.6 绝对接管误差矩阵

| 横向误差 | 航向误差 | 最大转向 | 饱和 | 最大路径误差 | 结果 |
|---:|---:|---:|---:|---:|---|
| 0.10 m | 5° | 322.019 | 0 | 0.149 | PASS |
| 0.15 m | 8° | 332.063 | 0 | 0.208 | PASS |
| 0.15 m | 10° | 332.142 | 0 | 0.213 | PASS |
| 0.20 m | 8° | 335.221 | 0 | 0.251 | PASS |
| 0.20 m | 10° | 338.624 | 0 | 0.267 | PASS |
| 0.25 m | 12° | 349.906 | 0 | 0.345 | PASS |
| 0.35 m | 18.3° | 365.549 | 0 | 0.471 | PASS |

结论：

```text
静态接管误差本身不足以解释实车长时间 ±400。
```

## 17.7 转向执行器动态模型

### 0.3 s 延迟 + 500 request/s

```text
MAX_ABS_ROUTE_STEERING: 400
MAX_ABS_APPLIED_STEERING: 400
指令饱和：0.8 s
MAX_ROUTE_PATH_ERROR: 0.428 m
PASS
```

### 0.5 s 延迟 + 300 request/s

```text
MAX_ABS_ROUTE_STEERING: 400
MAX_ABS_APPLIED_STEERING: 400
指令饱和：0.2 s
MAX_ROUTE_PATH_ERROR: 0.494 m
PASS
```

这些是压力模型，不是实车标定值。

## 17.8 全程 8 RPM

强滞后模型：

```text
MAX_ROUTE_PATH_ERROR: 0.444 m
ROUTE_SATURATION_TIME: 0.6 s
PASS
```

结论：

- 精度略有改善；
- 未完全消除指令饱和；
- 时间明显增加；
- 不应全程固定 8 RPM。

## 17.9 动态限速最终仿真

条件：

```text
接管误差：0.35 m / 18.3°
执行器延迟：0.5 s
执行器速率：300 request/s
动态限速：开启
```

结果：

| 指标 | 值 |
|---|---:|
| 任务 | PASS |
| 稳定性 | PASS |
| 控制器最大路线转向 | 400.0 |
| 控制器路线饱和 | 0.2 s |
| 路线阶段实际执行器最大转向 | 365.403 |
| 路线阶段实际执行器饱和 | 0 s |
| 最大路径误差 | 0.472 m |
| 终点位置误差 | 0.282 m |
| 终点航向误差 | 3.535° |

结论：

```text
控制器可能瞬间请求 400，
但动态限速使路线阶段实际执行器未进入饱和区。
```

---

# 18. 已知实车表现

## 18.1 较好运行

路线：

```text
7.28_test_02.yaml
```

表现：

- 长度 31.217 m；
- 接管误差约 0.08 m / 8.2°；
- 跟踪到 31.10/31.22 m；
- 急弯处多次 ±400；
- 最终能恢复。

## 18.2 问题路线

路线：

```text
7.31_test_01.yaml
```

表现：

- 长度约 69.573 m；
- 多次入轨失败和 ±400；
- 一次接管约 0.35 m / 18.3°；
- 当时约 20 RPM；
- 路径误差快速增至 1.5 m；
- 触发安全停车。

可能因素：

- 接管状态切换；
- 速度过高；
- 执行器延迟；
- 入轨遗留转向；
- GNSS/航向波动；
- 模型和实车转向不一致。

---

# 19. 当前未提交改动

最后确认：

```text
 M config/patrol_system.yaml
 M src/patrol_bringup/launch/patrol_system.launch.py
 M src/patrol_route_follower/patrol_route_follower/route_follower_node.py
 M tools/tests/test_absolute_full_mission.py
?? tools/tests/handoff_experiments/
```

主要内容：

- 速度 10～15 RPM；
- 接管恢复动态限速；
- 大转向自动降速；
- launch 参数映射；
- pose 等待修复；
- 接管误差矩阵；
- 弯道场景；
- 执行器滞后场景；
- 实际执行器路线阶段指标。

曾生成临时备份：

```text
/tmp/patrol_dynamic_speed_backup_20260803_174722
```

`/tmp` 不可靠，不得作为永久备份。

---

# 20. 已知问题和风险

## P0：实车前必须处理

### 20.1 动态限速未完成全回归

必须回归：

- 直线；
- 2 m 弯；
- 1.5 m 弯；
- 接管矩阵；
- 完整任务；
- 实际路线；
- GNSS hold；
- 动态加载；
- 停止/重启；
- 模式切换。

### 20.2 当前工作区未提交

先保存 patch，再继续。

### 20.3 转向未实车标定

需要测：

- 响应延迟；
- 变化速率；
- 死区；
- 回差；
- 左右对称；
- 请求与实际角度；
- CAN 反馈可信度。

### 20.4 接管阈值未最终确定

当前：

```text
1.0 m / 180°
```

过宽松，但不能只依据理想仿真收紧。

### 20.5 障碍物安全未完成

禁止：

- 靠墙无人运行；
- 人车混行；
- 动态障碍；
- 无监护自动巡逻。

## P1：近期修复

### 20.6 command manager 关闭竞态

偶发 context invalid。

### 20.7 `0x701` 解析

速度和转向反馈可能不正确。

### 20.8 `0x703` 电池解析

电压、电流、SOC 曾明显异常。

### 20.9 MINS200 驱动退出

历史出现 shutdown 重入、析构异常。

### 20.10 日志持久化

正式测试不得只放 `/tmp`。

建议每次外场保存：

- Git SHA；
- git diff；
- 配置；
- route 副本；
- 参数 dump；
- CAN 状态；
- 定位状态；
- 命令；
- 车辆反馈；
- 任务日志；
- 测试结论。

## P2：产品化

- 前向动态避障；
- 后向检测；
- 道路边界；
- 碰撞地图；
- 摄像头录像；
- 异常识别；
- 电池管理；
- 远程告警；
- 自动回充；
- 开机自启；
- 长期可靠性；
- 磁盘轮转；
- 故障恢复。

---

# 21. 后续 Agent 任务路线图

## 阶段 A：接管快照

```bash
cd /home/nvidia/patrol_ws

SNAPSHOT="reports/agent_handoff_$(date +%Y%m%d_%H%M%S)"
mkdir -p "$SNAPSHOT"

git remote -v > "$SNAPSHOT/git_remote.txt"
git branch -vv > "$SNAPSHOT/git_branch.txt"
git status --short > "$SNAPSHOT/git_status.txt"
git log --graph --decorate --oneline --all -60 \
  > "$SNAPSHOT/git_log.txt"
git diff > "$SNAPSHOT/working_tree.patch"

cp config/patrol_system.yaml "$SNAPSHOT/"
cp config/route_smoothing.yaml "$SNAPSHOT/" 2>/dev/null || true
```

## 阶段 B：不改代码先编译

```bash
source /opt/ros/humble/setup.bash
source /home/nvidia/ros2_humble_main/install/setup.bash

colcon build --symlink-install
```

## 阶段 C：动态限速回归

顺序：

1. `test_absolute_full_mission.py`
2. 直线场景
3. 2 m 弯
4. 1.5 m 弯
5. 绝对接管矩阵
6. 强执行器滞后
7. GNSS hold 测试
8. 动态路线加载
9. 常驻运行时
10. 干净退出

建议通过标准：

```text
MISSION_SUCCEEDED: True
MISSION_FAILED: False
ROUTE_END_ERROR <= 0.35 m
ROUTE_HEADING_ERROR_DEG <= 10°
实际执行器饱和时间 <= 1 s
无未处理异常
```

## 阶段 D：提交动态限速

建议分支：

```text
feature/route-handoff-dynamic-speed
```

提交：

- route follower；
- YAML；
- launch 参数；
- 测试；
- 文档。

不要提交：

- `/tmp`；
- 大日志；
- 地图密钥；
- 摄像头密码；
- 临时备份；
- 未确认可公开的真实坐标。

## 阶段 E：转向标定

建议请求序列：

```text
0
+50
+100
+200
+300
+400
0
-50
-100
-200
-300
-400
0
```

记录：

- 指令时间；
- CAN 反馈；
- 轮胎开始动作；
- 达到稳定时间；
- 实际角度；
- 回零；
- 左右差异。

## 阶段 F：短路线实车

路线要求：

- 5～10 m；
- 直线或大半径弯；
- 起终点宽敞；
- 最大 8～10 RPM；
- 前后无障碍。

流程：

```text
底层检查
→ 静止定位
→ 手动控制
→ 只规划入轨
→ 人工检查路径
→ 自动任务
→ 全量日志
→ 立即复盘
```

## 阶段 G：69.57 m 路线

只有短路线通过后测试：

```text
7.31_test_01.yaml
```

速度：

```text
10 RPM
→ 12 RPM
→ 15 RPM
```

每次只提升一级。

## 阶段 H：安全层

1. 确认 Livox 类型；
2. PointCloud2 适配；
3. 前向 ROI；
4. 地面过滤；
5. 障碍距离；
6. 分级减速；
7. 紧急停车；
8. 后向检测；
9. Hybrid A* 碰撞；
10. 道路边界。

---

# 22. 实车测试纪律与验收建议

## 22.1 测试顺序

```text
T0 编译
→ T1 离线完整任务
→ T2 上层启动
→ T3 室外定位
→ T4 手动控制
→ T5 路线录制
→ T6 只规划入轨
→ T7 短路线自动任务
→ T8 长路线
```

任何阶段失败，不进入下一阶段。

## 22.2 定位检查

静止至少 60 秒，记录：

- x/y 漂移；
- yaw 漂移；
- NSV1/NSV2；
- Nav_st；
- 数据年龄；
- 跳变。

## 22.3 控制发布者

```bash
ros2 topic info -v /vehicle/command
```

必须：

- Publisher count = 1；
- 发布者为 `patrol_command_manager`；
- 订阅者为 `vehicle_interface_node`。

## 22.4 建议验收指标

具体最终值由用户确认，当前建议：

| 项目 | 建议 |
|---|---|
| 路线完成 | 100% |
| 人工接管 | 0 次 |
| 终点位置误差 | ≤ 0.35 m |
| 终点航向误差 | ≤ 10° |
| 跟踪安全阈值 | 不触发 1.5 m |
| 持续转向饱和 | 不允许 |
| 高频左右振荡 | 不允许 |
| 重复测试 | 多次一致 |
| GNSS 弱化 | 超界立即停车 |
| 障碍物 | 前后均有安全闭环后才无人运行 |

---

# 23. Agent 接手第一天清单

```text
[ ] 阅读本文件
[ ] 确认 /home/nvidia/patrol_ws
[ ] 确认未 source 旧 yuan_ws
[ ] git remote -v
[ ] git branch -vv
[ ] git status --short
[ ] 保存 working_tree.patch
[ ] 读取全部 patrol_system.yaml
[ ] 读取全部 route_smoothing.yaml
[ ] 全量编译
[ ] Python 语法检查
[ ] 基础离线完整任务
[ ] 动态限速回归
[ ] 检查 command manager 关闭竞态
[ ] 补充仓库 URL、PR、Issue
[ ] 形成参数完整 dump
[ ] 回归通过后提交独立分支
[ ] 制定实车转向标定方案
```

---

# 24. 常用命令速查

## 环境

```bash
source /opt/ros/humble/setup.bash
source /home/nvidia/ros2_humble_main/install/setup.bash
source /home/nvidia/patrol_ws/install/setup.bash
```

## 编译

```bash
cd /home/nvidia/patrol_ws
colcon build --symlink-install
```

## 底层

```bash
./scripts/start_base.sh
```

## 手动

```bash
./scripts/manual_control.sh
```

## 录制

```bash
./scripts/record_route.sh 路线名称
```

## 复现

```bash
./scripts/replay_route.sh 路线名称
```

## 停止任务

```bash
./scripts/stop_mission.sh
```

## 关闭全部

```bash
./scripts/stop_all.sh
```

## 状态

```bash
./scripts/status.sh
```

## 地图

```bash
./scripts/start_patrol_map.sh
```

## 离线任务

```bash
python3 tools/tests/test_absolute_full_mission.py
```

## CAN

```bash
ip -details -statistics link show can0
timeout 10 candump -e -x can0
```

## 最终控制源

```bash
ros2 topic info -v /vehicle/command
```

## 定位

```bash
ros2 topic echo /patrol/localization_status --once
ros2 topic echo /patrol/pose --once
```

## 参数

```bash
ros2 param dump /patrol_route_follower
ros2 param dump /patrol_entry_executor
ros2 param dump /patrol_localization
```

## Git

```bash
git status --short
git branch -vv
git log --graph --decorate --oneline --all -40
git diff --stat
```

---

# 25. 必须现场补充的信息

本次交接无法直接读取 Jetson 当前文件系统和 GitHub 网页，因此接手 Agent 必须补充：

1. `git remote -v` 真实地址；
2. 当前分支和 HEAD；
3. 2026-08-03 后的新提交；
4. `patrol_system.yaml` 全部实时值；
5. `route_smoothing.yaml` 全部实时值；
6. 全部节点运行参数 dump；
7. 路线文件清单；
8. 脚本清单；
9. GitHub PR/Issue/Actions；
10. `/vehicle/status` 消息定义；
11. CAN 协议源码版本；
12. Livox 真实消息类型；
13. `/scan` 是否可用；
14. 摄像头 RTSP 路径和凭据管理；
15. 实车转向标定；
16. 电池 BMS 原始 CAN；
17. 最新外场测试结果。

追加格式：

```markdown
## 现场接手补充记录

- 日期：
- Agent：
- 分支：
- HEAD：
- 仓库：
- 工作区状态：
- 新发现：
- 证据路径：
- 后续动作：
```

---

# 26. 最终交接结论

当前项目已经从早期单节点路线跟踪原型重构为较完整的 ROS 2 自主巡逻架构，已具备：

- 稳定路线原点；
- WGS84 绝对坐标路线；
- 稳定起终点；
- 路线平滑和地图设计；
- Hybrid A* 自动入轨；
- 前进、倒车、换向；
- Pure Pursuit 正式跟踪；
- 单调路线弧长；
- 可靠终点基础判定；
- STOP/MANUAL/AUTO；
- 自动命令复用；
- 总任务状态机；
- 常驻运行时；
- 动态路线加载；
- 地图前端；
- GNSS 短时弱化保护；
- 基础 CAN 安全控制；
- 完整离线仿真；
- 部分实车验证；
- 接管低速恢复和大转向动态限速原型。

当前最重要的近期路径是：

```text
保存并审查未提交改动
→ 动态限速完整回归
→ 提交独立分支
→ 实车转向标定
→ 低速短路线实车
→ 69.57 m 路线逐级提速
→ Livox 前后向安全闭环
```

在转向标定、动态避障、后向检测和长期实车回归完成前：

```text
本项目不具备无人值守巡逻条件。
```

后续 Agent 必须坚持：

- 事实优先；
- 一次只解决一个问题；
- 先仿真后实车；
- 每次有日志；
- 每次可回滚；
- 不删除用户数据；
- 不猜硬件协议；
- 安全停车优先于任务完成。
